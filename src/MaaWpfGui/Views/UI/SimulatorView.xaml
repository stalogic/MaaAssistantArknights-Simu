<UserControl
    x:Class="MaaWpfGui.Views.UI.SimulatorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="clr-namespace:CalcBinding;assembly=CalcBinding"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:s="https://github.com/canton7/Stylet"
    xmlns:ui_vms="clr-namespace:MaaWpfGui.ViewModels.UI"
    d:Background="White"
    d:DataContext="{d:DesignInstance {x:Type ui_vms:SimulatorViewModel}}"
    d:DesignHeight="600"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- AI Control Panel -->
        <Border
            Grid.Row="0"
            Padding="15"
            Background="{DynamicResource RegionBrushOpacity25}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="4">
            <StackPanel>
                <TextBlock
                    Margin="0,0,0,10"
                    FontSize="16"
                    FontWeight="Bold"
                    Text="AI Control Panel" />

                <!-- Enable Toggle -->
                <DockPanel Margin="0,5">
                    <TextBlock
                        Width="120"
                        VerticalAlignment="Center"
                        Text="AI Enable:" />
                    <CheckBox IsChecked="{Binding AiEnabled}" Content="Enable AI Connection" />
                </DockPanel>

                <!-- Endpoint -->
                <DockPanel Margin="0,5">
                    <TextBlock
                        Width="120"
                        VerticalAlignment="Center"
                        Text="AI Endpoint:" />
                    <TextBox
                        Text="{Binding AiEndpoint, UpdateSourceTrigger=PropertyChanged}"
                        IsEnabled="{Binding AiEnabled}"
                        hc:InfoElement.Placeholder="http://localhost:8765" />
                </DockPanel>

                <!-- Status -->
                <DockPanel Margin="0,5">
                    <TextBlock
                        Width="120"
                        VerticalAlignment="Center"
                        Text="Status:" />
                    <StackPanel Orientation="Horizontal">
                        <TextBlock VerticalAlignment="Center" Text="{Binding AiStatus}" />
                        <Button
                            Margin="10,0,0,0"
                            Padding="5,2"
                            Command="{s:Action CheckAiConnection}"
                            Content="Check" />
                    </StackPanel>
                </DockPanel>
            </StackPanel>
        </Border>

        <!-- Placeholder for future content -->
        <TextBlock
            Grid.Row="2"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="24"
            Foreground="{DynamicResource GrayBrush}"
            Text="Simulator" />
    </Grid>
</UserControl>
